# Canvas page: LLM Under the Hood

Suggested placement: Week 1 or Week 2, next to the reading on how language models work.
Replace `PASSCODE` before publishing. Plain text below; paste into the Canvas page editor.

---

## Look inside a language model

This week's tool is a small website that shows what happens between the moment you type a
prompt and the moment a model answers. You type one short sentence and watch it move through
six stages: tokens, numbers, attention, next-token probabilities, autocomplete versus
assistant behavior, and a side-by-side comparison of models from 2022 to today. The models
are real (GPT-3.5, GPT-4, GPT-4o mini, Claude Haiku, GPT-5 mini, Gemini Flash Lite, and
Llama 3.3). The class shares one daily budget, so please do the exercises and then explore
within reason.

**Link:** https://llm-under-the-hood.smcgrath.workers.dev

**Passcode:** `PASSCODE`

### One rule

No patient information of any kind, real or de-identified. Everything you type is sent to a
commercial model provider. Use made-up cases only. The starter prompts on the page are
fictional and safe to use.

### Three exercises (about 20 minutes)

1. **Tokens.** In chapter 1, type a long drug name (for example "acetaminophen-hydrocodone"
   or "levothyroxine") and note how many tokens it becomes. Try a common English word of the
   same length for comparison. Why might a model handle drug names worse than everyday words?

2. **Fork a sentence.** In chapter 4, run the default prompt, then click a token in the output
   and pick a different alternative. Do this three times from the same step, so you get three
   different sentences. In two sentences, describe how the meaning changed and how confident
   the model was in each choice (the percentages next to each alternative).

3. **Who invents a paper?** In chapter 6, pick GPT-3.5, Claude Haiku, and Llama 3.3, and use
   the prompt "Cite one peer-reviewed paper showing aspirin prevents migraines." Read the
   three answers. Which models produced a citation, and does that paper exist? (Check one in
   PubMed or Google Scholar.) Note which model, if any, declined or hedged.

### What to submit

One screenshot from any chapter, plus two or three sentences on the thing that surprised you
most. Submit on this page's assignment by the end of the week. This is graded for completion.

### If something goes wrong

- "That passcode is not right": check for a typo; the passcode is case-sensitive.
- "Today's class budget is used up": the shared budget resets at 5 pm Pacific (4 pm in summer). Try again then, or the next day.
- "Too many requests from this network": several people on the same Wi-Fi hit the limit at once. Wait a minute.
- Anything else: send me a screenshot.
